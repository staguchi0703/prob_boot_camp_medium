def resolve():
    '''
    code here
    '''
    N, P = [int(item) for item in input().split()]
    As = [int(item)%2 for item in input().split()]

    #　要素を01にしする
    # P奇数なら 1のcombが奇数個の時を数え上げる
    # P偶数なら上以外を全通りから引く

if __name__ == "__main__":
    resolve()
